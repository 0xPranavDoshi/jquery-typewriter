!(function (t) {
  const e = (t) => new Promise((e) => setTimeout(e, t));
  t.fn.typeWrite = function (s, l) {
    return this.each(async function () {
      var i = t(this).text();
      (letters = i.split("")), t(this).text("");
      for (var r = letters[0]; 1 == l; ) {
        for (let l = 0; l < letters.length; l++)
          void 0 !== letters[l + 1]
            ? (t(this).text(r),
              (r += letters[l + 1]),
              console.log(r),
              await e(s))
            : (t(this).text(r), (r += letters[l]), await e(s));
        console.log("Clearing text"), t(this).text(""), (r = letters[0]);
      }
      for (let l = 0; l < letters.length; l++)
        void 0 !== letters[l + 1]
          ? (t(this).text(r), (r += letters[l + 1]), console.log(r), await e(s))
          : (t(this).text(r), (r += letters[l]), await e(s));
    });
  };
})(jQuery);
